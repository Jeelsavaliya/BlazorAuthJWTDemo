@* @page "/register"

<h3>Register</h3>

<EditForm Model="@user" >
    <DataAnnotationsValidator />
    <ValidationSummary />

    <InputText @bind-Value="user.Name" placeholder="Name" /><br/>
    <InputText @bind-Value="user.Email" placeholder="Email" /><br />
    <InputText @bind-Value="user.Password" placeholder="Password" type="password" /><br />
    <InputText @bind-Value="user.ConfirmPassword" placeholder="Password" type="ConfirmPassword" />

    <button type="submit">Register</button>
</EditForm> *@

@page "/register"

<h2>Register</h2>

<EditForm Model="@user" OnValidSubmit="@RegisterSuccess">
    <DataAnnotationsValidator />
    <ValidationSummary />
    <div>
        <label>Name:</label>
        <InputText @bind-Value="user.Name" />
    </div>
    <div>
        <label>Email:</label>
        <InputText @bind-Value="user.Email" />
    </div>
    <div>
        <label>Password:</label>
        <InputText @bind-Value="user.Password" type="password" />
    </div>
    <div>
        <label>Confirm Password:</label>
        <InputText @bind-Value="user.ConfirmPassword" type="password" />
    </div>
    <button type="submit">Register</button>
</EditForm>

